apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grocy
  namespace: grocy
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - grocy.apps.home.arpa
      secretName: apps-home-arpa-tls
  rules:
    - host: grocy.apps.home.arpa
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grocy
                port:
                  number: 80
